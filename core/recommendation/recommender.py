def recommend_jobs(resume_text: str, job_texts: list[str], top_k: int = 5) -> list[dict]:
    """
    Recommend top matching jobs for a resume.

    Args:
        resume_text (str)
        job_texts (list[str])
        top_k (int)

    Returns:
        list[dict]: Ranked job matches
    """
    pass
